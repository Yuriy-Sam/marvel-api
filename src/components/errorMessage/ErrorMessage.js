import error  from '../../resources/img/error.gif';


const ErrorMessage = () => {
    return <img style={{height: '250px', margin: '0 auto'} } src={error} alt="spinner" />;
}

export default ErrorMessage;